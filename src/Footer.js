import React from 'react';
import './Footer.css';

function Footer() {
    return (
        <footer className="footer">
            {/* Empty footer for now */}
        </footer>
    );
}

export default Footer;
